<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Food item Edit Page</title>
  </head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    <h1>Add a new food item</h1>

    <form action="/users/<%= user._id %>/foods" method="POST"> 
        <label for="name">Name:</label>
        <input type="text" name="name" id="name" required/>

        <label for="isExpired">Expired?</label>
        <input type="checkbox" name="isExpired" id="isExpired">
    
      <button type="submit">Add new Food Item</button>
    </form>
  </body>